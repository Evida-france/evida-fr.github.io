import {jsonHeaders} from "../lib/orders.mjs";import {quoteShipping} from "../lib/cj.mjs";
export async function handler(event){const headers=jsonHeaders(event.headers?.origin||"");if(event.httpMethod==="OPTIONS")return{statusCode:204,headers,body:""};if(event.httpMethod!=="POST")return{statusCode:405,headers,body:JSON.stringify({error:"Méthode non autorisée."})};try{const b=JSON.parse(event.body||"{}");const quote=await quoteShipping(b.customer||{},Array.isArray(b.items)?b.items:[]);return{statusCode:200,headers,body:JSON.stringify({quote})}}catch(e){return{statusCode:400,headers,body:JSON.stringify({error:e.message||"Calcul de livraison impossible."})}}}
