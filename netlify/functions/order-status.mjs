import {configureBlobs,jsonHeaders,orderStore,publicOrder,sendConfirmation,sendShippingConfirmation,verifySumUpCheckout} from "../lib/orders.mjs";
import {fulfillPaidOrderWithCJ,syncCJOrder} from "../lib/cj.mjs";
export async function handler(event){configureBlobs(event);const headers=jsonHeaders(event.headers?.origin||"");if(event.httpMethod==="OPTIONS")return{statusCode:204,headers,body:""};if(event.httpMethod!=="GET")return{statusCode:405,headers,body:JSON.stringify({error:"Méthode non autorisée."})};try{const id=String(event.queryStringParameters?.order||""),token=String(event.queryStringParameters?.token||""),store=orderStore();let o=await store.get(id,{type:"json"});if(!o||!token||token!==o.accessToken)return{statusCode:404,headers,body:JSON.stringify({error:"Commande introuvable."})};if(o.paymentStatus==="PENDING"&&o.checkoutId){const c=await verifySumUpCheckout(o.checkoutId);if(c?.status){o.paymentStatus=c.status;if(c.status==="PAID"){o.status="paid";o=await sendConfirmation(o);try{o=await fulfillPaidOrderWithCJ(o)}catch(e){o.cj={...(o.cj||{}),status:"error",error:e.message}}}}}if(o.paymentStatus==="PAID"){o=await syncCJOrder(o);if(o.trackingNumber&&!o.shippingEmailSentAt)o=await sendShippingConfirmation(o)}await store.setJSON(id,o);return{statusCode:200,headers,body:JSON.stringify({order:publicOrder(o)})}}catch(e){return{statusCode:500,headers,body:JSON.stringify({error:"Impossible de vérifier la commande."})}}}
